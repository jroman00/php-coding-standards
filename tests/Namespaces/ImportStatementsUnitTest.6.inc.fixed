<?php

namespace App\Foo;
use Exception;
use Foo\Alpha;
use Foo\Bravo;
use Foo\Charlie;









class ExampleClass
{
    public function foo()
    {
        $foo = 'hello';

        $bar = function () use ($foo) {
            return $foo . ' world';
        };

        return $bar();
    }
}
